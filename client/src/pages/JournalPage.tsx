import { TrackerFrame } from "@/components/TrackerFrame";
import { formatDate } from "@/lib/tracker";

export default function JournalPage() { return <TrackerFrame>{state => { const entries = state.records.filter(record => record.journal).sort((a, b) => b.date.getTime() - a.date.getTime()); return <div className="mx-auto max-w-[980px] p-5 sm:p-7 lg:p-8"><p className="eyebrow text-[#d9ff3e]">Document the work</p><h1 className="mt-2 font-display text-3xl font-bold text-white">Daily journal</h1><p className="mt-2 text-sm text-white/45">A private history of the work you are putting in.</p><div className="mt-7 space-y-4">{entries.length ? entries.map(entry => <article className="surface p-5" key={entry.id}><p className="eyebrow">{formatDate(entry.date, { weekday: "long", month: "long", day: "numeric", year: "numeric" })}</p><p className="mt-3 whitespace-pre-wrap text-sm leading-7 text-white/75">{entry.journal}</p></article>) : <div className="surface p-8 text-center"><p className="text-sm text-white/50">Your journal is clear for now. Add today’s reflection from the dashboard.</p></div>}</div></div>; }}</TrackerFrame>; }
