import type { ChartPoint } from "@shared/tracker";
import { Area, AreaChart, CartesianGrid, ResponsiveContainer, Tooltip, XAxis, YAxis } from "recharts";
import { formatDate } from "@/lib/tracker";

export function ProgressChart({ data }: { data: ChartPoint[] }) {
  return <div className="surface grid-bg h-[330px] p-5 sm:p-6"><div className="mb-5 flex items-start justify-between"><div><p className="eyebrow">Cumulative performance</p><h2 className="mt-1 panel-title">Actual discipline, day by day</h2></div><div className="hidden gap-4 text-xs text-white/45 sm:flex"><span className="flex items-center gap-1.5"><i className="size-2 rounded-full bg-[#d9ff3e]" /> Actual</span><span className="flex items-center gap-1.5"><i className="size-2 rounded-full bg-[#8291ff]" /> Ideal pace</span></div></div><ResponsiveContainer width="100%" height="82%"><AreaChart data={data} margin={{ left: -16, right: 8, top: 4 }}><defs><linearGradient id="actualFill" x1="0" x2="0" y1="0" y2="1"><stop offset="0%" stopColor="#d9ff3e" stopOpacity={.28} /><stop offset="100%" stopColor="#d9ff3e" stopOpacity={0} /></linearGradient></defs><CartesianGrid stroke="rgba(255,255,255,.065)" vertical={false} /><XAxis dataKey="day" tick={{ fill: "rgba(255,255,255,.34)", fontSize: 11 }} tickLine={false} axisLine={false} tickFormatter={day => `D${day}`} /><YAxis domain={[0, 100]} tick={{ fill: "rgba(255,255,255,.34)", fontSize: 11 }} tickLine={false} axisLine={false} tickFormatter={value => `${value}%`} /><Tooltip cursor={{ stroke: "rgba(217,255,62,.4)", strokeWidth: 1 }} content={({ active, payload }) => { const point = payload?.[0]?.payload as ChartPoint | undefined; return active && point ? <div className="rounded-xl border border-white/[.1] bg-[#161b27] p-3 text-xs shadow-2xl"><p className="font-medium text-white">Day {point.day} · {formatDate(point.date)}</p><div className="mt-2 space-y-1 text-white/50"><p>Progress <b className="text-[#d9ff3e]">{point.actual}%</b></p><p>Daily score <b className="text-white">{point.dailyScore}/3</b></p><p>Streak <b className="text-white">{point.currentStreak} days</b></p></div></div> : null; }} /><Area type="monotone" dataKey="ideal" stroke="#8291ff" strokeDasharray="4 4" strokeWidth={1.5} fill="transparent" /><Area type="monotone" dataKey="actual" stroke="#d9ff3e" strokeWidth={2.5} fill="url(#actualFill)" /></AreaChart></ResponsiveContainer></div>;
}
